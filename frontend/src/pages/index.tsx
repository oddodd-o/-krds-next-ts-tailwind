import User from '@/components/user';
import React from 'react';

const Home = () => {
    return (
        <div className="py-10">
            <User />
        </div>
    );
};

export default Home;
